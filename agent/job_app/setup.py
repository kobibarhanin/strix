from setuptools import setup

setup(name='job_pack',
      version='',
      description='',
      url='',
      author='',
      author_email='',
      license='',
      packages=['job_pack'],
      # install_requires=['Flask'],
      zip_safe=False)
